const product_middleware = require('./product.middleware');
const user_middleware = require('./user.middleware');
const sale_middleware = require('./sale.middleware');

module.exports = Object.freeze({
    product_middleware,
    user_middleware,
    sale_middleware

})